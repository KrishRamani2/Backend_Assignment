# Server
PORT=3000
NODE_ENV=development

# Database (SQLite for dev — switch to PostgreSQL connection string for production)
DATABASE_URL="file:./dev.db"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="24h"

# Bcrypt
BCRYPT_SALT_ROUNDS=12
